spring:
  datasource:
    url: jdbc:tc:postgresql:14.0:///event-bot
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
